{
  "name": "color-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "watchify src/app.js -o public/app.js -t [ babelify --presets [ es2015 ] ]",
    "server": "cd public; live-server --port=1234 --entry-file=index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "live-server": "^1.0.0",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "mithril": "^0.2.5",
    "ramda": "^0.21.0"
  }
}
